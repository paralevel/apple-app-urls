# Apple app URL schemes for iOS 26 and macOS 26 Tahoe

| App Name | URL Scheme |
| --- | --- |
| _App Store_ | <pre><code>itms-apps://?action=today</code></pre> |
| _App Store > Updates_ | <pre><code>itms-apps://?action=updates</code></pre> |
| _App Store > Safari Extensions_ | <pre><code>itms-apps://safariExtensions</code></pre> |
| _App Store – search for 'example'_ | <pre><code>itms-apps://?action=search&term=example</code></pre> |
| _Apple Configurator <sup>(macOS)</sup>_ | <pre><code>configurator://</code></pre> |
| _Apps <sup>(macOS)</sup>_ | <pre><code>spotlight://apps</code></pre> |
| _Books_ | <pre><code>ibooks://</code></pre> |
| _Calculator_ | <pre><code>calc://</code></pre> |
| _Calendar <sup>(iOS)</sup>_ | <pre><code>calshow://</code></pre> |
| _Calendar <sup>(macOS)</sup>_ | <pre><code>ical://</code></pre> |
| _Calendar – add subscription calendar_ | <pre><code>webcal://</code></pre> |
| _Camera <sup>(iOS)</sup>_ | <pre><code>camera://</code></pre> |
| _Camera – take photo with back camera <sup>(iOS)</sup>_ | <pre><code>camera://configuration?capturemode=photo&capturedevice=back</code></pre> |
| _Camera – take photo with front camera <sup>(iOS)</sup>_ | <pre><code>camera://configuration?capturemode=photo&capturedevice=front</code></pre> |
| _Camera – shoot video with back camera <sup>(iOS)</sup>_ | <pre><code>camera://configuration?capturemode=video&capturedevice=back</code></pre> |
| _Camera – shoot video with front camera <sup>(iOS)</sup>_ | <pre><code>camera://configuration?capturemode=video&capturedevice=front</code></pre> |
| _Clock_ | <pre><code>clock-worldclock://</code></pre> |
| _Contacts <sup>(iOS)</sup>_ | <pre><code>contact://</code></pre> |
| _Contacts <sup>(macOS)</sup>_ | <pre><code>addressbook://</code></pre> |
| _Dictionary <sup>(macOS)</sup>_ | <pre><code>dict://</code></pre> |
| _Dictionary – search for 'example' <sup>(macOS)</sup>_ | <pre><code>dict://example</code></pre> |
| _FaceTime_ | <pre><code>facetime://</code></pre> |
| _Files <sup>(iOS)</sup>_ | <pre><code>shareddocuments://</code></pre> |
| _Find My_ | <pre><code>findmy://</code></pre> |
| _Find My > Find My Friends_ | <pre><code>findmyfriends://</code></pre> |
| _Finder <sup>(macOS)</sup>_ | <pre><code>file:///Users/Shared</code></pre> |
| _Fitness <sup>(iOS)</sup>_ | <pre><code>fitnessapp://</code></pre> |
| _Font Book <sup>(macOS)</sup>_ | <pre><code>fontbook://</code></pre> |
| _Freeform_ | <pre><code>freeform://</code></pre> |
| _Games_ | <pre><code>games://</code></pre> |
| _Health <sup>(iOS)</sup>_ | <pre><code>x-argonaut-app://</code></pre> |
| _iTunes Store <sup>(iOS)</sup>_ | <pre><code>itms://</code></pre> |
| _Journal_ | <pre><code>moments://</code></pre> |
| _Keynote <sup>(macOS)</sup>_ | <pre><code>com.apple.iwork.keynote-share://</code></pre> |
| _Magnifier <sup>(iOS)</sup>_ | <pre><code>apple-magnifier://</code></pre> |
| _Mail <sup>(iOS)</sup>_ | <pre><code>com.apple.mobilemail://</code></pre> |
| _Mail – compose new e-mail_ | <pre><code>mailto://</code></pre> |
| _Maps_ | <pre><code>maps://</code></pre> |
| _Messages_ | <pre><code>iChat://</code></pre> |
| _Messages – create new message_ | <pre><code>sms://</code></pre> |
| _Music_ | <pre><code>music://</code></pre> |
| _Notes_ | <pre><code>applenotes://</code></pre> |
| _Notes – create new note <sup>(iOS)</sup>_ | <pre><code>mobilenotes://newNote</code></pre> |
| _Notes – create new note <sup>(macOS)</sup>_ | <pre><code>notes://newNote?displayMode=paper</code></pre> |
| _Numbers <sup>(macOS)</sup>_ | <pre><code>com.apple.iwork.numbers-share://</code></pre> |
| _Pages <sup>(macOS)</sup>_ | <pre><code>com.apple.iwork.pages-share://</code></pre> |
| _Passwords_ | <pre><code>apple-otpauth://</code></pre> |
| _Phone <sup>(macOS)</sup>_ | <pre><code>phoneapp://</code></pre> |
| _Phone > Favorites <sup>(iOS)</sup>_ | <pre><code>mobilephone-favorites://</code></pre> |
| _Phone > Calls (Unified mode) or Recents (Classic mode) <sup>(iOS)</sup>_ | <pre><code>mobilephone-recents://</code></pre> |
| _Phone > Contacts <sup>(iOS)</sup>_ | <pre><code>mobilephone-contacts://</code></pre> |
| _Phone > Keypad <sup>(iOS)</sup>_ | <pre><code>mobilephone-keypad://</code></pre> |
| _Photos_ | <pre><code>photos://</code></pre> |
| _Photos > Library (tab) <sup>(iOS)</sup>_ | <pre><code>photos://library</code></pre> |
| _Photos > Collections (tab) <sup>(iOS)</sup>_ | <pre><code>photos://collections</code></pre> |
| _Photos – search for 'example' <sup>(iOS)</sup>_ | <pre><code>photos-navigation://search?searchTerm=example</code></pre> |
| _Photos > most recent photo_ | <pre><code>photos://lastasset</code></pre> |
| _Photos > Recently Saved_ | <pre><code>photos://album?name=recently-saved</code></pre> |
| _Photos > Recently Deleted_ | <pre><code>photos://album?name=recently-deleted</code></pre> |
| _Photos > Favorites_ | <pre><code>photos://album?name=favorites</code></pre> |
| _Photos > Featured Photos <sup>(iOS)</sup>_ | <pre><code>photos://featuredPhoto</code></pre> |
| _Photos > Duplicates_ | <pre><code>photos://album?name=duplicates</code></pre> |
| _Photos > Memories_ | <pre><code>photos://memory-generation</code></pre> |
| _Photos > People & Pets_ | <pre><code>photos://people</code></pre> |
| _Photos > Maps_ | <pre><code>photos://places</code></pre> |
| _Podcasts_ | <pre><code>podcasts://</code></pre> |
| _Reminders_ | <pre><code>x-apple-reminderkit://</code></pre> |
| _Safari – search for 'example' on DuckDuckGo <sup>(iOS)</sup>_ | <pre><code>x-web-search://duckduckgo/?example</code></pre> |
| _Safari – search for 'example' on Google <sup>(iOS)</sup>_ | <pre><code>x-web-search://google/?example</code></pre> |
| _Safari – search for 'example' on Bing <sup>(iOS)</sup>_ | <pre><code>x-web-search://bing/?example</code></pre> |
| _Safari – search for 'example' on Yahoo <sup>(iOS)</sup>_ | <pre><code>x-web-search://yahoo/?example</code></pre> |
| _Safari – search for 'example' on Ecosia <sup>(iOS)</sup>_ | <pre><code>x-web-search://ecosia/?example</code></pre> |
| _Safari – search for 'example' on Wikipedia <sup>(iOS)</sup>_ | <pre><code>x-web-search://wikipedia/?example</code></pre> |
| _Settings <sup>(iOS)</sup>_ | <pre><code>prefs://</code></pre> |
| _Shortcuts_ | <pre><code>shortcuts://</code></pre> |
| _Shortcuts – run shortcut 'example'_ | <pre><code>shortcuts://run-shortcut?name=example</code></pre> |
| _Shortcuts – run shortcut 'example' with 'my text' as input_ | <pre><code>shortcuts://run-shortcut?name=example&input=my%20text</code></pre> |
| _Shortcuts – run shortcut 'example' with the clipboard as input <sup>(iOS)</sup>_ | <pre><code>shortcuts://run-shortcut?name=example&input=clipboard</code></pre> |
| _Shortcuts – open shortcut 'example' <sup>(case-insensitive)</sup>_ | <pre><code>shortcuts://open-shortcut?name=example` |
| _Shortcuts – create new shortcut_ | <pre><code>shortcuts://create-shortcut</code></pre> |
| _Shortcuts – create new automation <sup>(iOS)</sup>_ | <pre><code>shortcuts://create-automation</code></pre> |
| _Shortcuts > Automations <sup>(iOS)</sup>_ | <pre><code>shortcuts://automations</code></pre> |
| _Shortcuts > Gallery_ | <pre><code>shortcuts://gallery</code></pre> |
| _Shortcuts – install shortcut using iCloud ID from public link_ | <pre><code>shortcuts://shortcuts/icloud_link_id</code></pre> |
| _Shortcuts `x-callback-url` with for example `create-shortcut`_ | <pre><code>shortcuts://x-callback-url/create-shortcut</code></pre> |
| _Stocks_ | <pre><code>stocks://</code></pre> |
| _System Settings <sup>(macOS)</sup>_ | <pre><code>x-apple.systempreferences://</code></pre> |
| _Tips_ | <pre><code>x-apple-tips://</code></pre> |
| _TV_ | <pre><code>videos://</code></pre> |
| _Voice Memos <sup>(iOS)</sup>_ | <pre><code>voicememos://</code></pre> |
| _Wallet <sup>(iOS)</sup>_ | <pre><code>wallet://</code></pre> |
| _Watch <sup>(iOS)</sup>_ | <pre><code>bridge://</code></pre> |
| _Watch > Face Gallery_ | <pre><code>facegallery://</code></pre> |
| _Weather_ | <pre><code>weather://</code></pre> |
| _Web App <sup>(iOS) *</sup>_ | <pre><code>webapp://</code></pre> |

\
_<sub>* Opens the least recent web app you have installed (for example through “Add To Home Screen” in Safari with “Open as Web App” enabled) – only works in shortcuts</sub>_
